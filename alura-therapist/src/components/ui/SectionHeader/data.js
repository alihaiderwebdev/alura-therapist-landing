// This component has no static data - it's a reusable UI component
// Data will be passed via props
export const sectionHeaderData = {
  // Default section header props can be stored here if needed
  defaultTag: "h2",
  defaultAlign: "center"
};